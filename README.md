tiledmerge is command line tool that take multiple [tiled](http://www.mapeditor.org/) maps and merge their tilesets.

Usage:

	java -jar tiledmerge-1.0.jar -o /output/tileset/path /input/tmx/map/path
